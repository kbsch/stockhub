{
  "version": 2,
  "buildCommand": "pwd && ls -la && cd packages/client && npm run build && cp -r dist ../../dist",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null
}
